import XLSX = require('../');
import testCommon = require('./Common.js');

const file = 'formula_stress_test.xlsx'

describe(file, function () {
    // Opening the file currently crashes node
    //testCommon(file);
})
